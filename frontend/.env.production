# Keep the frontend on same-origin in production.
# Worker deployment still needs CORE_API_BASE_URL to proxy the core runtime path:
# auth, onboarding, health, and public-data.
# AI_PROXY_* is optional fallback only and not part of the main runtime contract.
VITE_API_BASE_URL=
